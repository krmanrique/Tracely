FROM node:20-alpine

WORKDIR /app

# Instalar dependencias primero (capa cacheada)
COPY package*.json ./
RUN npm ci --only=production

# Copiar el código fuente
COPY . .

EXPOSE 3000

CMD ["node", "src/app.js"]
